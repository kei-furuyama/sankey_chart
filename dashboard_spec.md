# 子育て・介護関係26手続き オンライン化ダッシュボード 定義書

Power BI レポート `26_administrative_procedures_online.pbip` のセマンティックモデルおよびメジャー定義をまとめたドキュメントです。

---

## 1. プロジェクト概要

| 項目 | 内容 |
|---|---|
| 正式名称 | 子育て・介護関係26手続きのオンライン化取組状況 |
| データ出典 | デジタル庁「地方公共団体におけるオンライン化の取組状況」 |
| 対象時点 | 2026年3月時点 |
| 対象自治体 | 全国1,741市区町村 |
| 対象手続き | 子育て関係／介護関係の26手続きを含む、ライフイベント手続および処理件数の多い手続 |
| ファイル形式 | PBIP (Power BI Project) |
| ページ数 | 2ページ（都道府県一覧、市区町村一覧） |
| ページサイズ | 1920 × 1080 px |

### ステータス値の解釈

ソースデータの `オンライン化状況` 列には以下3種類の値が存在します。

| 値 | 意味 | 完了判定 |
|---|---|---|
| `○` | オンライン対応済み | 完了 |
| `ー` | 該当する手続きがない（非該当） | 完了扱い |
| `""`（空文字） | 未オンライン化 | 未完了 |

`ー` を「完了扱い」とすることで、そもそも扱わない手続きが未完了としてカウントされることを防いでいます。

---

## 2. データソース

### 2.1 ファイル

- **ソースファイル**: `data_unpivoted.csv` (UTF-8, 12列)
- **元データ**: `20260120_policies_administrative_procedures_online_01_.xlsx` をアンピボット加工したもの

### 2.2 カラム構成（12列）

| # | カラム名 | 型 | 説明 |
|---|---|---|---|
| 1 | コード | text | 自治体コード（都道府県・市区町村コード） |
| 2 | 地域ブロック | text | 北海道・東北、関東、中部、近畿、中国・四国、九州・沖縄 |
| 3 | 都道府県 | text | 都道府県名 |
| 4 | 団体名 | text | 市区町村名 |
| 5 | 団体名フリガナ | text | 市区町村名のフリガナ（半角カタカナ） |
| 6 | 団体区分 | text | 政令市・中核市等の区分 |
| 7 | 大カテゴリ | text | `a) 処理件数が多い手続` / `b) ライフイベント手続` |
| 8 | サブカテゴリ | text | `ア.子育て関係` / `イ.介護関係`（大カテゴリbの場合のみ） |
| 9 | 手続名 | text | 個別手続名（例: 児童手当等の受給資格…） |
| 10 | 重点手続 | text | 重点手続フラグ（`●` or 空） |
| 11 | オンライン化状況 | text | `○` / `ー` / 空文字 |
| 12 | その他手続数 | int64 | その他手続の件数 |

---

## 3. セマンティックモデル

モデルは **オンライン化状況**（ファクトテーブル）と **完了状況**（カレンダーテーブル相当の計算テーブル）の2テーブル構成です。テーブル間にリレーションシップは定義されていません（完了状況はドーナツチャート用の軸として独立して利用）。

### 3.1 テーブル一覧

| テーブル名 | 種別 | 行数の目安 | 用途 |
|---|---|---|---|
| `オンライン化状況` | インポート（CSV経由） | 約150万行 | メインのファクトテーブル |
| `完了状況` | 計算テーブル | 2行 | ドーナツチャートの軸（完了／未完了） |

### 3.2 `オンライン化状況` テーブル

**パーティション（Power Query / M）**

```m
let
    Source = Csv.Document(
        File.Contents("data_unpivoted.csv"),
        [Delimiter=",", Columns=12, Encoding=65001, QuoteStyle=QuoteStyle.Csv]
    ),
    PromotedHeaders = Table.PromoteHeaders(Source, [PromoteAllScalars=true]),
    ChangedTypes = Table.TransformColumnTypes(PromotedHeaders, {
        {"コード",        type text},
        {"地域ブロック",  type text},
        {"都道府県",      type text},
        {"団体名",        type text},
        {"団体名フリガナ", type text},
        {"団体区分",      type text},
        {"大カテゴリ",    type text},
        {"サブカテゴリ",  type text},
        {"手続名",        type text},
        {"重点手続",      type text},
        {"オンライン化状況", type text},
        {"その他手続数",  Int64.Type}
    })
in
    ChangedTypes
```

**カラム**

| カラム名 | データ型 | 集計 | 備考 |
|---|---|---|---|
| コード | string | none | 自治体コード（DISTINCTCOUNTの対象） |
| 地域ブロック | string | none | 6地方区分 |
| 都道府県 | string | none | スライサー・カード見出し等で使用 |
| 団体名 | string | none | ページ2のマトリクス列・スライサーで使用 |
| 団体名フリガナ | string | none | |
| 団体区分 | string | none | |
| 大カテゴリ | string | none | `a) 処理件数が多い手続` / `b) ライフイベント手続` |
| サブカテゴリ | string | none | `ア.子育て関係` / `イ.介護関係` |
| 手続名 | string | none | ページ2のマトリクス行で使用 |
| 重点手続 | string | none | |
| オンライン化状況 | string | none | `○` / `ー` / `""` |
| その他手続数 | int64 | sum | |

### 3.3 `完了状況` テーブル（計算テーブル）

**定義式**

```dax
UNION (
    ROW ( "ステータス", "完了",   "順序", 1 ),
    ROW ( "ステータス", "未完了", "順序", 2 )
)
```

**カラム**

| カラム名 | データ型 | 並び替え | 用途 |
|---|---|---|---|
| ステータス | string | `順序` 列で昇順 | ドーナツチャートのカテゴリ |
| 順序 | int64 | — | ステータス列の並び順を制御 |

### 3.4 モデル全般の設定

- **互換性レベル**: 1601
- **カルチャ**: `ja-JP`
- **暗黙的メジャー**: 無効化（`discourageImplicitMeasures: true`）
- **クエリ順序**: `オンライン化状況`

---

## 4. DAX メジャー一覧

全10メジャーを、格納テーブル別に以下に整理します。

### 4.1 `オンライン化状況` テーブル上のメジャー（9個）

#### 4.1.1 オンライン対応数

ステータスが空文字でない行（`○` または `ー`）を数えます。

```dax
-- オンライン化状況が空でない（対応済み or 該当なし）行数を返す
COUNTROWS ( FILTER ( 'オンライン化状況', 'オンライン化状況'[オンライン化状況] <> "" ) )
```

- **書式**: `#,##0`
- **用途**: `オンライン化率` メジャーの分子

#### 4.1.2 該当なし数

ステータスが `ー` の行を数えます。

```dax
-- ステータスが「ー」（該当なし）の行数を返す
COUNTROWS ( FILTER ( 'オンライン化状況', 'オンライン化状況'[オンライン化状況] = "ー" ) )
```

- **書式**: `#,##0`

#### 4.1.3 未対応数

ステータスが空文字（未オンライン化）の行を数えます。

```dax
-- ステータスが空文字（未オンライン化）の行数を返す
COUNTROWS ( FILTER ( 'オンライン化状況', 'オンライン化状況'[オンライン化状況] = "" ) )
```

- **書式**: `#,##0`
- **用途**: `子育て介護26手続完了自治体数` の判定（サブカテゴリ絞り込み時）

#### 4.1.4 オンライン化率

対応数 ÷ (対応数 + 未対応数)。該当なし（`ー`）は分母から除かれます。

```dax
-- オンライン対応数 / (対応数 + 未対応数) の割合を返す
VAR _Online = [オンライン対応数]
VAR _Total  = _Online + [未対応数]
RETURN
    DIVIDE ( _Online, _Total )
```

- **書式**: `0.0%`

#### 4.1.5 手続数

フィルタコンテキスト内の手続行数。

```dax
-- 現在のフィルタコンテキスト内の手続行数を返す
COUNTROWS ( 'オンライン化状況' )
```

- **書式**: `#,##0`

#### 4.1.6 自治体数

一意な自治体数。`コード` 列の DISTINCTCOUNT。

```dax
-- 現在のフィルタコンテキスト内の一意な自治体数を返す
DISTINCTCOUNT ( 'オンライン化状況'[コード] )
```

- **書式**: `#,##0`
- **用途**: 左パネルKPI「全自治体数」表示、完了率分母

#### 4.1.7 子育て介護26手続完了自治体数 ★主要メジャー

子育て・介護の26手続きすべてがオンライン化済み（または `ー` 該当なし扱い）の自治体を1、それ以外を0として合計します。

```dax
-- 子育て・介護26手続すべてオンライン化済みの自治体数を返す
-- 未対応数が0の自治体を「完了」としてカウントする
SUMX (
    VALUES ( 'オンライン化状況'[コード] ),
    VAR _NotOnline =
        CALCULATE (
            COUNTROWS ( 'オンライン化状況' ),
            'オンライン化状況'[サブカテゴリ] IN { "ア.子育て関係", "イ.介護関係" },
            'オンライン化状況'[オンライン化状況] = ""
        )
    RETURN
        IF ( _NotOnline = 0, 1, 0 )
)
```

- **書式**: `#,##0`
- **用途**: 左パネルKPI「完了自治体数」、ページ1 都道府県タイルの数値
- **判定ロジック**: 子育て・介護サブカテゴリに絞った上で、未オンライン化（空文字）の行数が0ならその自治体は完了。`ー`（該当なし）はカウント対象外なので、実質的に対応済み扱い。

#### 4.1.8 子育て介護26手続完了率

完了自治体数 ÷ 自治体数。

```dax
-- 26手続完了自治体数 / 全自治体数 の割合を返す
DIVIDE ( [子育て介護26手続完了自治体数], [自治体数] )
```

- **書式**: `0.0%`
- **用途**: ページ1 右グリッドの都道府県タイル（47個）の表示値

#### 4.1.9 子育て介護オンライン化率_自治体別

子育て・介護サブカテゴリに限定した手続きベースのオンライン化率。

```dax
-- 子育て・介護カテゴリに限定した自治体別オンライン化率を返す
VAR _Online =
    CALCULATE (
        COUNTROWS ( 'オンライン化状況' ),
        'オンライン化状況'[サブカテゴリ] IN { "ア.子育て関係", "イ.介護関係" },
        'オンライン化状況'[オンライン化状況] <> ""
    )
VAR _Total =
    CALCULATE (
        COUNTROWS ( 'オンライン化状況' ),
        'オンライン化状況'[サブカテゴリ] IN { "ア.子育て関係", "イ.介護関係" }
    )
RETURN
    DIVIDE ( _Online, _Total )
```

- **書式**: `0%`

#### 4.1.10 都道府県ラベル

「都道府県名(完了数 / 全自治体数)」形式のラベル。

```dax
-- 「都道府県名(完了数 / 全自治体数)」形式のラベル文字列を返す
VAR _Pref      = SELECTEDVALUE ( 'オンライン化状況'[都道府県] )
VAR _Completed = [子育て介護26手続完了自治体数]
VAR _Total     = [自治体数]
RETURN
    _Pref & "(" & _Completed & " / " & _Total & ")"
```

- **用途**: 都道府県別タイルの見出しテキスト

#### 4.1.11 ステータス表示

選択行のオンライン化状況テキスト。マトリクス表の値として使用。

```dax
-- 選択された行のオンライン化状況テキストを返す（複数行の場合は空文字）
SELECTEDVALUE ( 'オンライン化状況'[オンライン化状況], "" )
```

#### 4.1.12 ステータスコード

条件付き書式でアイコン表示する際の数値コード。

```dax
-- ステータスを数値コードに変換する（○/ー→1, 空→-1, その他→BLANK）
-- 条件付き書式でのアイコン表示に使用
SWITCH (
    SELECTEDVALUE ( 'オンライン化状況'[オンライン化状況] ),
    "○", 1,
    "ー", 1,
    "",  -1,
    BLANK ()
)
```

- **書式**: `0`
- **値の意味**: `1` = 完了（青●）、`-1` = 未対応（灰●）、`BLANK()` = 該当なし

### 4.2 `完了状況` テーブル上のメジャー（1個）

#### 4.2.1 完了状況値

ドーナツチャートで「完了」スライスに完了自治体数、「未完了」スライスに残り自治体数を返します。

```dax
-- ドーナツチャート用: 「完了」なら完了自治体数、「未完了」なら残り自治体数を返す
VAR _Status    = SELECTEDVALUE ( '完了状況'[ステータス] )
VAR _Completed = [子育て介護26手続完了自治体数]
VAR _Total     = [自治体数]
RETURN
    SWITCH (
        _Status,
        "完了",   _Completed,
        "未完了", _Total - _Completed
    )
```

- **書式**: `#,##0`
- **用途**: 左パネルのドーナツチャート

### 4.3 メジャー依存関係

```
自治体数 ─┬─> 子育て介護26手続完了率
          └─> 完了状況値
                ↑
子育て介護26手続完了自治体数 ─┬─> 子育て介護26手続完了率
                              ├─> 都道府県ラベル
                              └─> 完了状況値

オンライン対応数 ──> オンライン化率
未対応数 ─────────> オンライン化率
```

---

## 5. レポートページ構成

### 5.1 ページ1: 都道府県一覧

全47都道府県の完了率を一覧表示するランディングページ。

**レイアウト（1920 × 1080）**

| 要素 | 位置 (x, y) | サイズ (w × h) | 備考 |
|---|---|---|---|
| ヘッダータイトル | (40, 20) | 900 × 42 | タイトル+「｜都道府県一覧」 |
| ヘッダーライン | (20, 66) | 1880 × 2 | `#D8D8DB` |
| 左サマリーパネル | (30, 80) | 370 × 870 | ドーナツ + KPI |
| 右グリッド | (440, 82〜) | 243px stride × 6列 | 47県タイル+地域見出し |
| 日付注記 | (40, 960) | 250 × 20 | 2026年3月時点の数値 |
| クレジット | (1740, 960) | 160 × 20 | デジタル庁 |

**左サマリーパネルの構成**

1. 見出しテキスト「26手続き全てのオンライン化が完了した自治体の割合」
2. ドーナツチャート（260×260）
    - カテゴリ: `完了状況[ステータス]`
    - 値: `完了状況[完了状況値]`
    - 色: 完了=`#0017C1`、未完了=`#D8D8DB`
    - 内側半径比: 82%（ドーナツ）
    - ラベル: Percent of total（中央の%表示）
3. 見出しテキスト「26手続き全てのオンライン化が完了した自治体数／全自治体数」
4. カード: `子育て介護26手続完了自治体数`
5. 区切り線
6. カード: `自治体数`

**右グリッド（47都道府県タイル）**

- 6列構成: 北海道・東北 / 関東 / 中部 / 近畿 / 中国・四国 / 九州・沖縄
- 各タイルは `card` ビジュアル + 都道府県フィルタ
- 表示値: `子育て介護26手続完了率`
- タイトル: 都道府県名（Arial 12px bold）

### 5.2 ページ2: 市区町村一覧

都道府県を選択して、その配下の全市区町村における26手続きのオンライン化状況をマトリクス表示するページ。

**ページフィルタ**

- サブカテゴリ IN { `ア.子育て関係`, `イ.介護関係` }（26手続きに絞り込み）

**レイアウト**

| 要素 | 位置 (x, y) | サイズ (w × h) | 備考 |
|---|---|---|---|
| ヘッダータイトル | (40, 20) | 900 × 42 | タイトル+「｜市区町村一覧」 |
| ヘッダーライン | (20, 66) | 1880 × 2 | |
| 都道府県スライサー | (1580, 14) | 200 × 48 | ドロップダウン・単一選択 |
| 選択都道府県名カード | (50, 90) | 310 × 40 | — |
| 左サマリーパネル | (30, 135) | 350 × 710 | ページ1と同じ構造 |
| 凡例 | (400, 1020) | 700 × 25 | ● オンライン可 ● 不可 ー 非該当 |
| マトリクステーブル | (400, 80) | 1500 × 930 | ピボットテーブル |

**マトリクステーブルの構成**

- **行**: `サブカテゴリ` > `手続名`（階層表示）
- **列**: `団体名`（市区町村）
- **値**: `ステータス記号`（※generate_report.pyが参照。model.bimには未定義の可能性あり、`ステータス表示` の別名もしくは追加実装と思われる）
- 小計: 非表示（行・列ともに）
- 罫線: 縦横 `#EEEEEE`、1px
- 値テキスト: Arial 14px、`#0017C1`

> ⚠️ `ステータス記号` メジャーは `generate_report.py` のマトリクス定義で参照されていますが、`model.bim` には現在定義がありません。実運用時は `ステータス表示` と置き換えるか、別途 `ステータス記号` メジャーを定義する必要があります。

---

## 6. デザイン仕様

デジタル庁ダッシュボードデザインテンプレート（`CY23SU08` ベース + `Digital_Agency_Dashboard_Desig3362615343750506.json` カスタムテーマ）に準拠しています。

### 6.1 カラーパレット

| 用途 | カラー名 | HEX |
|---|---|---|
| ページ背景 | `BG_PAGE` | `#FFFFFF` |
| パネル背景 | `BG_PANEL` | `#F8F8FB` |
| アクセント（完了・強調） | `ACCENT_BLUE` | `#0017C1` |
| ニュートラルグレー（未完了・境界） | `NEUTRAL_GRAY` | `#D8D8DB` |
| テキスト主要 | `TEXT_PRIMARY` | `#1A1A1A` |
| テキスト副次 | `TEXT_SECONDARY` | `#666666` |
| テキスト抑制 | `TEXT_MUTED` | `#999999` |

### 6.2 タイポグラフィ

- **フォントファミリ**: Arial（全要素共通）
- **タイトル**: 20px bold
- **セクション見出し**: 13〜14px bold
- **KPI 数値**: 24〜32px（ページ別）
- **本文**: 11〜14px

### 6.3 形状・装飾

- **角丸半径**: `RADIUS = 20px`
- **カード背景**: `BG_PANEL` (`#F8F8FB`)
- **ドーナツ内側半径比**: 82%（細めリング）

### 6.4 ビジュアルネーミング規約

`generate_report.py` 内の命名規約: `p{page}_{type}_{id}`

| プレフィクス | 例 | 意味 |
|---|---|---|
| `p1_` / `p2_` | `p1_title`, `p2_matrix` | ページ1／ページ2 |
| `p1_pf_{NN}` | `p1_pf_03` | ページ1 都道府県タイル（00〜46） |
| `p1_rh_{N}` | `p1_rh_2` | ページ1 地域見出し（0〜5） |
| `p2_sl_*` | `p2_sl_pref` | ページ2 スライサー |
| `{prefix}_donut` | `p1_donut`, `p2_donut` | サマリーパネルのドーナツ |
| `{prefix}_cnt_*` | `p1_cnt_done`, `p1_cnt_all` | サマリーパネルのカウント |

---

## 7. ビルド手順

### 7.1 report.json の再生成

```bash
python3 generate_report.py
```

- 入力: `generate_report.py` 内の定義
- 出力: `26_administrative_procedures_online.Report/report.json`
- バリデーション: JSONラウンドトリップ、ビジュアル名重複チェック、`prototypeQuery.Version == 2` チェック

> **注意**: `report.json` は手動編集せず、常に `generate_report.py` を編集して再生成する。

### 7.2 Power BI Modeling MCP Server

セマンティックモデルをプログラム的に操作するための MCP サーバーが設定済み。`ToolSearch` 経由でメジャー／テーブル／カラム／DAXクエリ／リレーションシップ等の操作が可能。

---

## 8. 補足

### 8.1 `generate_report.py` と `model.bim` の差分

`generate_report.py` は以下のメジャーをビジュアル定義で参照していますが、`model.bim` 側の定義状況が異なります。

| メジャー名 | model.bim | generate_report.py | 備考 |
|---|---|---|---|
| `ステータス記号` | 未定義 | 参照あり（p2_matrix） | 要追加、または `ステータス表示` に置換 |
| `選択都道府県` | 未定義 | 参照あり（p2_pref_name） | 要追加（`SELECTEDVALUE ( '...'[都道府県] )` 想定） |

実運用前に model.bim 側にこれらを追加するか、`generate_report.py` のビジュアル参照先を既存メジャー（`ステータス表示` 等）に書き換える必要があります。
